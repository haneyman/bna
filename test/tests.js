test( "hello test", function() {
    ok( 2 ==2, "Needs to be equal!" );
});

test('getTripTimes()', function() {
    initialize();
    equal(1==1, true, "The value is true");
    //            console.log("Starting test*****************");
    //            var time = getTripTime("CONC", "EMBR", getCurrentTimeHHMMSS());
    //            equal(time, 2580);
});